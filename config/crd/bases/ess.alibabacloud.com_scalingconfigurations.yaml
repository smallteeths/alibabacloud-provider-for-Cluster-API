---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: scalingconfigurations.ess.alibabacloud.com
spec:
  group: ess.alibabacloud.com
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: ScalingConfiguration
    listKind: ScalingConfigurationList
    plural: scalingconfigurations
    singular: scalingconfiguration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScalingConfiguration is the Schema for the ScalingConfigurations
          API. Provides an ESS scaling configuration resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScalingConfigurationSpec defines the desired state of ScalingConfiguration
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  active:
                    description: Whether active current scaling configuration in the
                      specified scaling group. Default to false.
                    type: boolean
                  creditSpecification:
                    description: 'Performance mode of the t5 burstable instance. Valid
                      values: ''Standard'', ''Unlimited''.'
                    type: string
                  dataDisk:
                    description: DataDisk mappings to attach to ecs instance. See
                      data_disk below for details.
                    items:
                      properties:
                        autoSnapshotPolicyId:
                          description: The id of auto snapshot policy for data disk.
                          type: string
                        category:
                          description: Category of data disk. The parameter value
                            options are ephemeral_ssd, cloud_efficiency, cloud_ssd
                            and cloud.
                          type: string
                        deleteWithInstance:
                          description: 'Whether to delete data disks attached on ecs
                            when release ecs instance. Optional value: true or false,
                            default to true.'
                          type: boolean
                        description:
                          description: 'The description of data disk N. Valid values
                            of N: 1 to 16. The description must be 2 to 256 characters
                            in length and cannot start with http:// or https://.'
                          type: string
                        device:
                          description: 'The mount point of data disk N. Valid values
                            of N: 1 to 16. If this parameter is not specified, the
                            system automatically allocates a mount point to created
                            ECS instances. The name of the mount point ranges from
                            /dev/xvdb to /dev/xvdz in alphabetical order.'
                          type: string
                        encrypted:
                          description: 'Specifies whether data disk N is to be encrypted.
                            Valid values of N: 1 to 16. Valid values: true: encrypted,
                            false: not encrypted. Default value: false.'
                          type: boolean
                        kmsKeyId:
                          description: 'The CMK ID for data disk N. Valid values of
                            N: 1 to 16.'
                          type: string
                        name:
                          description: 'The name of data disk N. Valid values of N:
                            1 to 16. It must be 2 to 128 characters in length. It
                            must start with a letter and cannot start with http://
                            or https://. It can contain letters, digits, colons (:),
                            underscores (_), and hyphens (-). Default value: null.'
                          type: string
                        performanceLevel:
                          description: The performance level of the ESSD used as data
                            disk.
                          type: string
                        size:
                          description: Size of data disk, in GB. The value ranges
                            [5,2000] for a cloud disk, [5,1024] for an ephemeral disk,
                            [5,800] for an ephemeral_ssd disk, [20,32768] for cloud_efficiency,
                            cloud_ssd, cloud_essd disk.
                          type: number
                        snapshotId:
                          description: Snapshot used for creating the data disk. If
                            this parameter is specified, the size parameter is neglected,
                            and the size of the created disk is the size of the snapshot.
                          type: string
                      type: object
                    type: array
                  enable:
                    description: Whether enable the specified scaling group(make it
                      active) to which the current scaling configuration belongs.
                    type: boolean
                  forceDelete:
                    description: The last scaling configuration will be deleted forcibly
                      with deleting its scaling group. Default to false.
                    type: boolean
                  hostName:
                    description: Hostname of an ECS instance.
                    type: string
                  imageId:
                    description: ID of an image file, indicating the image resource
                      selected when an instance is enabled.
                    type: string
                  imageName:
                    description: Name of an image file, indicating the image resource
                      selected when an instance is enabled.
                    type: string
                  instanceIds:
                    description: (Deprecated) It has been deprecated from version
                      1.6.0. New resource alicloud_ess_attachment replaces it.
                    items:
                      type: string
                    type: array
                  instanceName:
                    description: Name of an ECS instance. Default to "ESS-Instance".
                      It is valid from version 1.7.1.
                    type: string
                  instancePatternInfo:
                    description: intelligent configuration mode. In this mode, you
                      only need to specify the number of vCPUs, memory size, instance
                      family, and maximum price. The system selects an instance type
                      that is provided at the lowest price based on your configurations
                      to create ECS instances. This mode is available only for scaling
                      groups that reside in virtual private clouds (VPCs). This mode
                      helps reduce the failures of scale-out activities caused by
                      insufficient inventory of instance types.  See instance_pattern_info
                      below for details.
                    items:
                      properties:
                        cores:
                          description: The number of vCPUs that are specified for
                            an instance type in instancePatternInfo.
                          type: number
                        instanceFamilyLevel:
                          description: The instance family level in instancePatternInfo.
                          type: string
                        maxPrice:
                          description: The maximum hourly price for a pay-as-you-go
                            instance or a preemptible instance in instancePatternInfo.
                          type: number
                        memory:
                          description: The memory size that is specified for an instance
                            type in instancePatternInfo.
                          type: number
                      type: object
                    type: array
                  instanceType:
                    description: Resource type of an ECS instance.
                    type: string
                  instanceTypeOverride:
                    description: specify the weight of instance type.  See instance_type_override
                      below for details.
                    items:
                      properties:
                        instanceType:
                          description: The is specified for an instance type in instanceTypeOverride.
                          type: string
                        weightedCapacity:
                          description: The weight of instance type in instanceTypeOverride.
                          type: number
                      type: object
                    type: array
                  instanceTypes:
                    description: Resource types of an ECS instance.
                    items:
                      type: string
                    type: array
                  internetChargeType:
                    description: 'Network billing type, Values: PayByBandwidth or
                      PayByTraffic. Default to PayByBandwidth.'
                    type: string
                  internetMaxBandwidthIn:
                    description: Maximum incoming bandwidth from the public network,
                      measured in Mbps (Mega bit per second). The value range is [1,200].
                    type: number
                  internetMaxBandwidthOut:
                    description: Maximum outgoing bandwidth from the public network,
                      measured in Mbps (Mega bit per second). The value range for
                      PayByBandwidth is [0,1024].
                    type: number
                  ioOptimized:
                    description: (Deprecated) It has been deprecated on instance resource.
                      All the launched alicloud instances will be I/O optimized.
                    type: string
                  isOutdated:
                    description: Whether to use outdated instance type. Default to
                      false.
                    type: boolean
                  keyName:
                    description: The name of key pair that can login ECS instance
                      successfully without password. If it is specified, the password
                      would be invalid.
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to a db account. If
                      the password is filled in, this field will be ignored.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating a db account with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  override:
                    description: Indicates whether to overwrite the existing data.
                      Default to false.
                    type: boolean
                  password:
                    description: 'The password of the ECS instance. The password must
                      be 8 to 30 characters in length. It must contains at least three
                      of the following character types: uppercase letters, lowercase
                      letters, digits, and special characters. Special characters
                      include () ~!@#$%^&*-_+=\|{}[]:;''<>,.?/, The password of Windows-based
                      instances cannot start with a forward slash (/).'
                    type: string
                  passwordInherit:
                    description: Specifies whether to use the password that is predefined
                      in the image. If the PasswordInherit parameter is set to true,
                      the password and kms_encrypted_password will be ignored. You
                      must ensure that the selected image has a password configured.
                    type: boolean
                  resourceGroupId:
                    description: ID of resource group.
                    type: string
                  roleName:
                    description: Instance RAM role name. The name is provided and
                      maintained by RAM. You can use alicloud_ram_role to create a
                      new one.
                    type: string
                  scalingConfigurationName:
                    description: Name shown for the scheduled task. which must contain
                      2-64 characters (English or Chinese), starting with numbers,
                      English letters or Chinese characters, and can contain number,
                      underscores _, hypens -, and decimal point .. If this parameter
                      value is not specified, the default value is ScalingConfigurationId.
                    type: string
                  scalingGroupId:
                    description: ID of the scaling group of a scaling configuration.
                    type: string
                  securityGroupId:
                    description: ID of the security group used to create new instance.
                      It is conflict with security_group_ids.
                    type: string
                  securityGroupIds:
                    description: List IDs of the security group used to create new
                      instances. It is conflict with security_group_id.
                    items:
                      type: string
                    type: array
                  spotPriceLimit:
                    description: Sets the maximum price hourly for instance types.
                      See spot_price_limit below for details.
                    items:
                      properties:
                        instanceType:
                          description: Resource type of an ECS instance.
                          type: string
                        priceLimit:
                          description: Price limit hourly of instance type, 2 decimals
                            is allowed at most.
                          type: number
                      type: object
                    type: array
                  spotStrategy:
                    description: 'The spot strategy for a Pay-As-You-Go instance.
                      Valid values: NoSpot, SpotAsPriceGo, SpotWithPriceLimit.'
                    type: string
                  substitute:
                    description: The another scaling configuration which will be active
                      automatically and replace current configuration when setting
                      active to 'false'. It is invalid when active is 'true'.
                    type: string
                  systemDiskAutoSnapshotPolicyId:
                    description: The id of auto snapshot policy for system disk.
                    type: string
                  systemDiskCategory:
                    description: Category of the system disk. The parameter value
                      options are ephemeral_ssd, cloud_efficiency, cloud_ssd, cloud_essd
                      and cloud. cloud only is used to some no I/O optimized instance.
                      Default to cloud_efficiency.
                    type: string
                  systemDiskDescription:
                    description: The description of the system disk. The description
                      must be 2 to 256 characters in length and cannot start with
                      http:// or https://.
                    type: string
                  systemDiskEncrypted:
                    description: Whether to encrypt the system disk.
                    type: boolean
                  systemDiskName:
                    description: 'The name of the system disk. It must be 2 to 128
                      characters in length. It must start with a letter and cannot
                      start with http:// or https://. It can contain letters, digits,
                      colons (:), underscores (_), and hyphens (-). Default value:
                      null.'
                    type: string
                  systemDiskPerformanceLevel:
                    description: The performance level of the ESSD used as the system
                      disk.
                    type: string
                  systemDiskSize:
                    description: 'Size of system disk, in GiB. Optional values: cloud:
                      20-500, cloud_efficiency: 20-500, cloud_ssd: 20-500, ephemeral_ssd:
                      20-500 The default value is max{40, ImageSize}. If this parameter
                      is set, the system disk size must be greater than or equal to
                      max{40, ImageSize}.'
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource. It will
                      be applied for ECS instances finally.
                    type: object
                    x-kubernetes-map-type: granular
                  userData:
                    description: User-defined data to customize the startup behaviors
                      of the ECS instance and to pass data into the ECS instance.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  active:
                    description: Whether active current scaling configuration in the
                      specified scaling group. Default to false.
                    type: boolean
                  creditSpecification:
                    description: 'Performance mode of the t5 burstable instance. Valid
                      values: ''Standard'', ''Unlimited''.'
                    type: string
                  dataDisk:
                    description: DataDisk mappings to attach to ecs instance. See
                      data_disk below for details.
                    items:
                      properties:
                        autoSnapshotPolicyId:
                          description: The id of auto snapshot policy for data disk.
                          type: string
                        category:
                          description: Category of data disk. The parameter value
                            options are ephemeral_ssd, cloud_efficiency, cloud_ssd
                            and cloud.
                          type: string
                        deleteWithInstance:
                          description: 'Whether to delete data disks attached on ecs
                            when release ecs instance. Optional value: true or false,
                            default to true.'
                          type: boolean
                        description:
                          description: 'The description of data disk N. Valid values
                            of N: 1 to 16. The description must be 2 to 256 characters
                            in length and cannot start with http:// or https://.'
                          type: string
                        device:
                          description: 'The mount point of data disk N. Valid values
                            of N: 1 to 16. If this parameter is not specified, the
                            system automatically allocates a mount point to created
                            ECS instances. The name of the mount point ranges from
                            /dev/xvdb to /dev/xvdz in alphabetical order.'
                          type: string
                        encrypted:
                          description: 'Specifies whether data disk N is to be encrypted.
                            Valid values of N: 1 to 16. Valid values: true: encrypted,
                            false: not encrypted. Default value: false.'
                          type: boolean
                        kmsKeyId:
                          description: 'The CMK ID for data disk N. Valid values of
                            N: 1 to 16.'
                          type: string
                        name:
                          description: 'The name of data disk N. Valid values of N:
                            1 to 16. It must be 2 to 128 characters in length. It
                            must start with a letter and cannot start with http://
                            or https://. It can contain letters, digits, colons (:),
                            underscores (_), and hyphens (-). Default value: null.'
                          type: string
                        performanceLevel:
                          description: The performance level of the ESSD used as data
                            disk.
                          type: string
                        size:
                          description: Size of data disk, in GB. The value ranges
                            [5,2000] for a cloud disk, [5,1024] for an ephemeral disk,
                            [5,800] for an ephemeral_ssd disk, [20,32768] for cloud_efficiency,
                            cloud_ssd, cloud_essd disk.
                          type: number
                        snapshotId:
                          description: Snapshot used for creating the data disk. If
                            this parameter is specified, the size parameter is neglected,
                            and the size of the created disk is the size of the snapshot.
                          type: string
                      type: object
                    type: array
                  enable:
                    description: Whether enable the specified scaling group(make it
                      active) to which the current scaling configuration belongs.
                    type: boolean
                  forceDelete:
                    description: The last scaling configuration will be deleted forcibly
                      with deleting its scaling group. Default to false.
                    type: boolean
                  hostName:
                    description: Hostname of an ECS instance.
                    type: string
                  imageId:
                    description: ID of an image file, indicating the image resource
                      selected when an instance is enabled.
                    type: string
                  imageName:
                    description: Name of an image file, indicating the image resource
                      selected when an instance is enabled.
                    type: string
                  instanceIds:
                    description: (Deprecated) It has been deprecated from version
                      1.6.0. New resource alicloud_ess_attachment replaces it.
                    items:
                      type: string
                    type: array
                  instanceName:
                    description: Name of an ECS instance. Default to "ESS-Instance".
                      It is valid from version 1.7.1.
                    type: string
                  instancePatternInfo:
                    description: intelligent configuration mode. In this mode, you
                      only need to specify the number of vCPUs, memory size, instance
                      family, and maximum price. The system selects an instance type
                      that is provided at the lowest price based on your configurations
                      to create ECS instances. This mode is available only for scaling
                      groups that reside in virtual private clouds (VPCs). This mode
                      helps reduce the failures of scale-out activities caused by
                      insufficient inventory of instance types.  See instance_pattern_info
                      below for details.
                    items:
                      properties:
                        cores:
                          description: The number of vCPUs that are specified for
                            an instance type in instancePatternInfo.
                          type: number
                        instanceFamilyLevel:
                          description: The instance family level in instancePatternInfo.
                          type: string
                        maxPrice:
                          description: The maximum hourly price for a pay-as-you-go
                            instance or a preemptible instance in instancePatternInfo.
                          type: number
                        memory:
                          description: The memory size that is specified for an instance
                            type in instancePatternInfo.
                          type: number
                      type: object
                    type: array
                  instanceType:
                    description: Resource type of an ECS instance.
                    type: string
                  instanceTypeOverride:
                    description: specify the weight of instance type.  See instance_type_override
                      below for details.
                    items:
                      properties:
                        instanceType:
                          description: The is specified for an instance type in instanceTypeOverride.
                          type: string
                        weightedCapacity:
                          description: The weight of instance type in instanceTypeOverride.
                          type: number
                      type: object
                    type: array
                  instanceTypes:
                    description: Resource types of an ECS instance.
                    items:
                      type: string
                    type: array
                  internetChargeType:
                    description: 'Network billing type, Values: PayByBandwidth or
                      PayByTraffic. Default to PayByBandwidth.'
                    type: string
                  internetMaxBandwidthIn:
                    description: Maximum incoming bandwidth from the public network,
                      measured in Mbps (Mega bit per second). The value range is [1,200].
                    type: number
                  internetMaxBandwidthOut:
                    description: Maximum outgoing bandwidth from the public network,
                      measured in Mbps (Mega bit per second). The value range for
                      PayByBandwidth is [0,1024].
                    type: number
                  ioOptimized:
                    description: (Deprecated) It has been deprecated on instance resource.
                      All the launched alicloud instances will be I/O optimized.
                    type: string
                  isOutdated:
                    description: Whether to use outdated instance type. Default to
                      false.
                    type: boolean
                  keyName:
                    description: The name of key pair that can login ECS instance
                      successfully without password. If it is specified, the password
                      would be invalid.
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to a db account. If
                      the password is filled in, this field will be ignored.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating a db account with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  override:
                    description: Indicates whether to overwrite the existing data.
                      Default to false.
                    type: boolean
                  password:
                    description: 'The password of the ECS instance. The password must
                      be 8 to 30 characters in length. It must contains at least three
                      of the following character types: uppercase letters, lowercase
                      letters, digits, and special characters. Special characters
                      include () ~!@#$%^&*-_+=\|{}[]:;''<>,.?/, The password of Windows-based
                      instances cannot start with a forward slash (/).'
                    type: string
                  passwordInherit:
                    description: Specifies whether to use the password that is predefined
                      in the image. If the PasswordInherit parameter is set to true,
                      the password and kms_encrypted_password will be ignored. You
                      must ensure that the selected image has a password configured.
                    type: boolean
                  resourceGroupId:
                    description: ID of resource group.
                    type: string
                  roleName:
                    description: Instance RAM role name. The name is provided and
                      maintained by RAM. You can use alicloud_ram_role to create a
                      new one.
                    type: string
                  scalingConfigurationName:
                    description: Name shown for the scheduled task. which must contain
                      2-64 characters (English or Chinese), starting with numbers,
                      English letters or Chinese characters, and can contain number,
                      underscores _, hypens -, and decimal point .. If this parameter
                      value is not specified, the default value is ScalingConfigurationId.
                    type: string
                  scalingGroupId:
                    description: ID of the scaling group of a scaling configuration.
                    type: string
                  securityGroupId:
                    description: ID of the security group used to create new instance.
                      It is conflict with security_group_ids.
                    type: string
                  securityGroupIds:
                    description: List IDs of the security group used to create new
                      instances. It is conflict with security_group_id.
                    items:
                      type: string
                    type: array
                  spotPriceLimit:
                    description: Sets the maximum price hourly for instance types.
                      See spot_price_limit below for details.
                    items:
                      properties:
                        instanceType:
                          description: Resource type of an ECS instance.
                          type: string
                        priceLimit:
                          description: Price limit hourly of instance type, 2 decimals
                            is allowed at most.
                          type: number
                      type: object
                    type: array
                  spotStrategy:
                    description: 'The spot strategy for a Pay-As-You-Go instance.
                      Valid values: NoSpot, SpotAsPriceGo, SpotWithPriceLimit.'
                    type: string
                  substitute:
                    description: The another scaling configuration which will be active
                      automatically and replace current configuration when setting
                      active to 'false'. It is invalid when active is 'true'.
                    type: string
                  systemDiskAutoSnapshotPolicyId:
                    description: The id of auto snapshot policy for system disk.
                    type: string
                  systemDiskCategory:
                    description: Category of the system disk. The parameter value
                      options are ephemeral_ssd, cloud_efficiency, cloud_ssd, cloud_essd
                      and cloud. cloud only is used to some no I/O optimized instance.
                      Default to cloud_efficiency.
                    type: string
                  systemDiskDescription:
                    description: The description of the system disk. The description
                      must be 2 to 256 characters in length and cannot start with
                      http:// or https://.
                    type: string
                  systemDiskEncrypted:
                    description: Whether to encrypt the system disk.
                    type: boolean
                  systemDiskName:
                    description: 'The name of the system disk. It must be 2 to 128
                      characters in length. It must start with a letter and cannot
                      start with http:// or https://. It can contain letters, digits,
                      colons (:), underscores (_), and hyphens (-). Default value:
                      null.'
                    type: string
                  systemDiskPerformanceLevel:
                    description: The performance level of the ESSD used as the system
                      disk.
                    type: string
                  systemDiskSize:
                    description: 'Size of system disk, in GiB. Optional values: cloud:
                      20-500, cloud_efficiency: 20-500, cloud_ssd: 20-500, ephemeral_ssd:
                      20-500 The default value is max{40, ImageSize}. If this parameter
                      is set, the system disk size must be greater than or equal to
                      max{40, ImageSize}.'
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource. It will
                      be applied for ECS instances finally.
                    type: object
                    x-kubernetes-map-type: granular
                  userData:
                    description: User-defined data to customize the startup behaviors
                      of the ECS instance and to pass data into the ECS instance.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.scalingGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.scalingGroupId)
                || (has(self.initProvider) && has(self.initProvider.scalingGroupId))'
          status:
            description: ScalingConfigurationStatus defines the observed state of
              ScalingConfiguration.
            properties:
              atProvider:
                properties:
                  active:
                    description: Whether active current scaling configuration in the
                      specified scaling group. Default to false.
                    type: boolean
                  creditSpecification:
                    description: 'Performance mode of the t5 burstable instance. Valid
                      values: ''Standard'', ''Unlimited''.'
                    type: string
                  dataDisk:
                    description: DataDisk mappings to attach to ecs instance. See
                      data_disk below for details.
                    items:
                      properties:
                        autoSnapshotPolicyId:
                          description: The id of auto snapshot policy for data disk.
                          type: string
                        category:
                          description: Category of data disk. The parameter value
                            options are ephemeral_ssd, cloud_efficiency, cloud_ssd
                            and cloud.
                          type: string
                        deleteWithInstance:
                          description: 'Whether to delete data disks attached on ecs
                            when release ecs instance. Optional value: true or false,
                            default to true.'
                          type: boolean
                        description:
                          description: 'The description of data disk N. Valid values
                            of N: 1 to 16. The description must be 2 to 256 characters
                            in length and cannot start with http:// or https://.'
                          type: string
                        device:
                          description: 'The mount point of data disk N. Valid values
                            of N: 1 to 16. If this parameter is not specified, the
                            system automatically allocates a mount point to created
                            ECS instances. The name of the mount point ranges from
                            /dev/xvdb to /dev/xvdz in alphabetical order.'
                          type: string
                        encrypted:
                          description: 'Specifies whether data disk N is to be encrypted.
                            Valid values of N: 1 to 16. Valid values: true: encrypted,
                            false: not encrypted. Default value: false.'
                          type: boolean
                        kmsKeyId:
                          description: 'The CMK ID for data disk N. Valid values of
                            N: 1 to 16.'
                          type: string
                        name:
                          description: 'The name of data disk N. Valid values of N:
                            1 to 16. It must be 2 to 128 characters in length. It
                            must start with a letter and cannot start with http://
                            or https://. It can contain letters, digits, colons (:),
                            underscores (_), and hyphens (-). Default value: null.'
                          type: string
                        performanceLevel:
                          description: The performance level of the ESSD used as data
                            disk.
                          type: string
                        size:
                          description: Size of data disk, in GB. The value ranges
                            [5,2000] for a cloud disk, [5,1024] for an ephemeral disk,
                            [5,800] for an ephemeral_ssd disk, [20,32768] for cloud_efficiency,
                            cloud_ssd, cloud_essd disk.
                          type: number
                        snapshotId:
                          description: Snapshot used for creating the data disk. If
                            this parameter is specified, the size parameter is neglected,
                            and the size of the created disk is the size of the snapshot.
                          type: string
                      type: object
                    type: array
                  enable:
                    description: Whether enable the specified scaling group(make it
                      active) to which the current scaling configuration belongs.
                    type: boolean
                  forceDelete:
                    description: The last scaling configuration will be deleted forcibly
                      with deleting its scaling group. Default to false.
                    type: boolean
                  hostName:
                    description: Hostname of an ECS instance.
                    type: string
                  id:
                    description: The scaling configuration ID.
                    type: string
                  imageId:
                    description: ID of an image file, indicating the image resource
                      selected when an instance is enabled.
                    type: string
                  imageName:
                    description: Name of an image file, indicating the image resource
                      selected when an instance is enabled.
                    type: string
                  instanceIds:
                    description: (Deprecated) It has been deprecated from version
                      1.6.0. New resource alicloud_ess_attachment replaces it.
                    items:
                      type: string
                    type: array
                  instanceName:
                    description: Name of an ECS instance. Default to "ESS-Instance".
                      It is valid from version 1.7.1.
                    type: string
                  instancePatternInfo:
                    description: intelligent configuration mode. In this mode, you
                      only need to specify the number of vCPUs, memory size, instance
                      family, and maximum price. The system selects an instance type
                      that is provided at the lowest price based on your configurations
                      to create ECS instances. This mode is available only for scaling
                      groups that reside in virtual private clouds (VPCs). This mode
                      helps reduce the failures of scale-out activities caused by
                      insufficient inventory of instance types.  See instance_pattern_info
                      below for details.
                    items:
                      properties:
                        cores:
                          description: The number of vCPUs that are specified for
                            an instance type in instancePatternInfo.
                          type: number
                        instanceFamilyLevel:
                          description: The instance family level in instancePatternInfo.
                          type: string
                        maxPrice:
                          description: The maximum hourly price for a pay-as-you-go
                            instance or a preemptible instance in instancePatternInfo.
                          type: number
                        memory:
                          description: The memory size that is specified for an instance
                            type in instancePatternInfo.
                          type: number
                      type: object
                    type: array
                  instanceType:
                    description: Resource type of an ECS instance.
                    type: string
                  instanceTypeOverride:
                    description: specify the weight of instance type.  See instance_type_override
                      below for details.
                    items:
                      properties:
                        instanceType:
                          description: The is specified for an instance type in instanceTypeOverride.
                          type: string
                        weightedCapacity:
                          description: The weight of instance type in instanceTypeOverride.
                          type: number
                      type: object
                    type: array
                  instanceTypes:
                    description: Resource types of an ECS instance.
                    items:
                      type: string
                    type: array
                  internetChargeType:
                    description: 'Network billing type, Values: PayByBandwidth or
                      PayByTraffic. Default to PayByBandwidth.'
                    type: string
                  internetMaxBandwidthIn:
                    description: Maximum incoming bandwidth from the public network,
                      measured in Mbps (Mega bit per second). The value range is [1,200].
                    type: number
                  internetMaxBandwidthOut:
                    description: Maximum outgoing bandwidth from the public network,
                      measured in Mbps (Mega bit per second). The value range for
                      PayByBandwidth is [0,1024].
                    type: number
                  ioOptimized:
                    description: (Deprecated) It has been deprecated on instance resource.
                      All the launched alicloud instances will be I/O optimized.
                    type: string
                  isOutdated:
                    description: Whether to use outdated instance type. Default to
                      false.
                    type: boolean
                  keyName:
                    description: The name of key pair that can login ECS instance
                      successfully without password. If it is specified, the password
                      would be invalid.
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to a db account. If
                      the password is filled in, this field will be ignored.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating a db account with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  override:
                    description: Indicates whether to overwrite the existing data.
                      Default to false.
                    type: boolean
                  password:
                    description: 'The password of the ECS instance. The password must
                      be 8 to 30 characters in length. It must contains at least three
                      of the following character types: uppercase letters, lowercase
                      letters, digits, and special characters. Special characters
                      include () ~!@#$%^&*-_+=\|{}[]:;''<>,.?/, The password of Windows-based
                      instances cannot start with a forward slash (/).'
                    type: string
                  passwordInherit:
                    description: Specifies whether to use the password that is predefined
                      in the image. If the PasswordInherit parameter is set to true,
                      the password and kms_encrypted_password will be ignored. You
                      must ensure that the selected image has a password configured.
                    type: boolean
                  resourceGroupId:
                    description: ID of resource group.
                    type: string
                  roleName:
                    description: Instance RAM role name. The name is provided and
                      maintained by RAM. You can use alicloud_ram_role to create a
                      new one.
                    type: string
                  scalingConfigurationName:
                    description: Name shown for the scheduled task. which must contain
                      2-64 characters (English or Chinese), starting with numbers,
                      English letters or Chinese characters, and can contain number,
                      underscores _, hypens -, and decimal point .. If this parameter
                      value is not specified, the default value is ScalingConfigurationId.
                    type: string
                  scalingGroupId:
                    description: ID of the scaling group of a scaling configuration.
                    type: string
                  securityGroupId:
                    description: ID of the security group used to create new instance.
                      It is conflict with security_group_ids.
                    type: string
                  securityGroupIds:
                    description: List IDs of the security group used to create new
                      instances. It is conflict with security_group_id.
                    items:
                      type: string
                    type: array
                  spotPriceLimit:
                    description: Sets the maximum price hourly for instance types.
                      See spot_price_limit below for details.
                    items:
                      properties:
                        instanceType:
                          description: Resource type of an ECS instance.
                          type: string
                        priceLimit:
                          description: Price limit hourly of instance type, 2 decimals
                            is allowed at most.
                          type: number
                      type: object
                    type: array
                  spotStrategy:
                    description: 'The spot strategy for a Pay-As-You-Go instance.
                      Valid values: NoSpot, SpotAsPriceGo, SpotWithPriceLimit.'
                    type: string
                  substitute:
                    description: The another scaling configuration which will be active
                      automatically and replace current configuration when setting
                      active to 'false'. It is invalid when active is 'true'.
                    type: string
                  systemDiskAutoSnapshotPolicyId:
                    description: The id of auto snapshot policy for system disk.
                    type: string
                  systemDiskCategory:
                    description: Category of the system disk. The parameter value
                      options are ephemeral_ssd, cloud_efficiency, cloud_ssd, cloud_essd
                      and cloud. cloud only is used to some no I/O optimized instance.
                      Default to cloud_efficiency.
                    type: string
                  systemDiskDescription:
                    description: The description of the system disk. The description
                      must be 2 to 256 characters in length and cannot start with
                      http:// or https://.
                    type: string
                  systemDiskEncrypted:
                    description: Whether to encrypt the system disk.
                    type: boolean
                  systemDiskName:
                    description: 'The name of the system disk. It must be 2 to 128
                      characters in length. It must start with a letter and cannot
                      start with http:// or https://. It can contain letters, digits,
                      colons (:), underscores (_), and hyphens (-). Default value:
                      null.'
                    type: string
                  systemDiskPerformanceLevel:
                    description: The performance level of the ESSD used as the system
                      disk.
                    type: string
                  systemDiskSize:
                    description: 'Size of system disk, in GiB. Optional values: cloud:
                      20-500, cloud_efficiency: 20-500, cloud_ssd: 20-500, ephemeral_ssd:
                      20-500 The default value is max{40, ImageSize}. If this parameter
                      is set, the system disk size must be greater than or equal to
                      max{40, ImageSize}.'
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource. It will
                      be applied for ECS instances finally.
                    type: object
                    x-kubernetes-map-type: granular
                  userData:
                    description: User-defined data to customize the startup behaviors
                      of the ECS instance and to pass data into the ECS instance.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
